#User function Template for python3

class Solution:
    def isDigitSumPalindrome(self,N):
        #code here
        sum = 0
        for i in str(N):
            sum+=int(i)
        a = str(sum)
        if a == a[::-1]:
            return 1
        else:
            return 0
 